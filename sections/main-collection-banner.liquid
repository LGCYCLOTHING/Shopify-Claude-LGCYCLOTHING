{% comment %}theme-check-disable ImgLazyLoading{% endcomment %}
{{ 'component-collection-hero.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .collection-hero {
    background: #faf8f4 !important;
  }

  @media screen and (max-width: 749px) {
    .collection-hero {
      background: #f0f0f0 !important;
    }
    .collection-hero--with-image .collection-hero__inner {
      padding-bottom: calc({{ settings.media_shadow_vertical_offset | at_least: 0 }}px + 2rem);
    }
  }

  .collection-hero__inner {
    padding-top: 4px !important;
    padding-bottom: 4px !important;
  }
  @media (max-width: 749px) {
    .collection-hero__inner {
      padding-top: 4px !important;
      padding-bottom: 4px !important;
    }
  }

  .lgcy-breadcrumb {
    display: none !important;
  }

  .collection-hero__title {
    font-family: 'Inter', system-ui, -apple-system, sans-serif !important;
    font-weight: 900 !important;
    letter-spacing: -0.03em !important;
    font-size: clamp(1.9rem, 4.2vw, 3.2rem) !important;
    color: #2a2a2a !important;
  }

  /* Same "New" flag treatment as the Shop panel / homepage hero — plain
     text, no badge chrome, muted via opacity so it reads as a subtle flag
     rather than competing with the heading. */
  .lgcy-collection-title-new {
    display: inline-block;
    position: relative;
    top: -1em;
    margin-left: 5px;
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    font-size: 0.42em;
    font-weight: 900;
    letter-spacing: 0.01em;
    text-transform: uppercase;
    color: rgba(0, 0, 0, 0.45);
    white-space: nowrap;
  }

  .collection-hero__description,
  .collection-hero__description p,
  .collection-hero__description li {
    font-family: 'Inter', system-ui, -apple-system, sans-serif !important;
    font-weight: 300 !important;
    font-size: 1.2rem !important;
    line-height: 1.7 !important;
    letter-spacing: 0.02em !important;
    color: #000000 !important;
  }
  .collection-hero__description p {
    margin: 0 0 14px 0 !important;
  }
  .collection-hero__description p:last-child {
    margin-bottom: 0 !important;
  }

  /* Collapsed by default on all screen sizes — a couple lines show, plus a
     hint of the next line fading out at the bottom (mask-image) rather than
     a hard cut, so there's a visible "glow" of more text before expanding. */
  .collection-hero__description {
    max-width: 950px;
    max-height: 3.6em;
    overflow: hidden;
    position: relative;
    transition: max-height 0.35s ease, -webkit-mask-image 0.35s ease, mask-image 0.35s ease;
    -webkit-mask-image: linear-gradient(to bottom, #000 30%, transparent 95%);
            mask-image: linear-gradient(to bottom, #000 30%, transparent 95%);
  }
  .collection-hero__description.is-expanded {
    max-height: 1000px;
    -webkit-mask-image: none;
            mask-image: none;
  }

  .collection-hero__desc-toggle {
    display: none;
    margin-top: 10px;
    background: none;
    border: none;
    padding: 0;
    font-family: 'Inter', sans-serif;
    font-weight: 600;
    font-size: 0.9rem;
    letter-spacing: 0.02em;
    color: #000000;
    text-decoration: underline;
    cursor: pointer;
  }

  @media (max-width: 749px) {
    .collection-hero__description {
      max-width: 100%;
    }
  }
{%- endstyle -%}

{%- liquid
  assign collection_display_title = collection.title
  if collection.handle == 'all'
    assign collection_display_title = 'All Products'
  endif
-%}
<div class="collection-hero{% if section.settings.show_collection_image and collection.image and collection.handle != 'lgcy-glasses' %} collection-hero--with-image{% endif %} color-{{ section.settings.color_scheme }} gradient">
  <div class="collection-hero__inner page-width {% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
    <div class="collection-hero__text-wrapper">
      <nav class="lgcy-breadcrumb" aria-label="Breadcrumb">
        <a href="{{ routes.root_url }}">Home</a>
        <span class="lgcy-breadcrumb__sep" aria-hidden="true">&rsaquo;</span>
        <span class="lgcy-breadcrumb__current">{{ collection_display_title | escape }}</span>
      </nav>

      <h1 class="collection-hero__title">
        <span class="visually-hidden">{{ 'sections.collection_template.title' | t }}: </span>
        {{- collection_display_title | escape -}}
        {%- if collection.handle == 'the-legacy-collection' -%}
          <span class="lgcy-collection-title-new">New</span>
        {%- endif -%}
      </h1>

      {%- if section.settings.show_collection_description -%}
        <div class="collection-hero__description rte" id="CollectionDesc-{{ section.id }}">{{ collection.description }}</div>
        <button
          type="button"
          class="collection-hero__desc-toggle"
          id="CollectionDescToggle-{{ section.id }}"
          aria-expanded="false"
          aria-controls="CollectionDesc-{{ section.id }}"
        >Read more</button>
        <script>
          (function () {
            var desc = document.getElementById('CollectionDesc-{{ section.id }}');
            var toggle = document.getElementById('CollectionDescToggle-{{ section.id }}');
            if (!desc || !toggle) return;

            function checkOverflow() {
              if (desc.classList.contains('is-expanded')) return;
              toggle.style.display = desc.scrollHeight > desc.clientHeight + 2 ? 'inline-block' : 'none';
            }

            toggle.addEventListener('click', function () {
              var expanded = desc.classList.toggle('is-expanded');
              toggle.textContent = expanded ? 'Read less' : 'Read more';
              toggle.setAttribute('aria-expanded', expanded ? 'true' : 'false');
            });

            window.addEventListener('resize', checkOverflow);
            checkOverflow();
            /* Inter loads via an async @import, so the first check above can
               run against fallback-font metrics and under-measure the line
               count — recheck once the real font is in so late reflow isn't
               left showing a faded cut-off with no way to expand it. */
            if (document.fonts && document.fonts.ready) {
              document.fonts.ready.then(checkOverflow);
            }
            setTimeout(checkOverflow, 500);
          })();
        </script>
      {%- endif -%}
    </div>

    {%- if section.settings.show_collection_image and collection.image and collection.handle != 'lgcy-glasses' -%}
      <div class="collection-hero__image-container media gradient">
        <img
          srcset="
            {%- if collection.image.width >= 165 -%}{{ collection.image | image_url: width: 165 }} 165w,{%- endif -%}
            {%- if collection.image.width >= 360 -%}{{ collection.image | image_url: width: 360 }} 360w,{%- endif -%}
            {%- if collection.image.width >= 535 -%}{{ collection.image | image_url: width: 535 }} 535w,{%- endif -%}
            {%- if collection.image.width >= 750 -%}{{ collection.image | image_url: width: 750 }} 750w,{%- endif -%}
            {%- if collection.image.width >= 1070 -%}{{ collection.image | image_url: width: 1070 }} 1070w,{%- endif -%}
            {%- if collection.image.width >= 1500 -%}{{ collection.image | image_url: width: 1500 }} 1500w,{%- endif -%}
            {{ collection.image | image_url }} {{ collection.image.width }}w
          "
          src="{{ collection.image | image_url: width: 750 }}"
          sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 100 | divided_by: 2 }}px, (min-width: 750px) calc(50vw - 130px), calc(50vw - 55px)"
          alt="{{ collection.image.alt | escape }}"
          width="{{ collection.image.width }}"
          height="{{ collection.image.height }}"
        >
      </div>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "t:sections.main-collection-banner.name",
  "class": "section",
  "settings": [
    {
      "type": "paragraph",
      "content": "t:sections.main-collection-banner.settings.paragraph.content"
    },
    {
      "type": "checkbox",
      "id": "show_collection_description",
      "default": true,
      "label": "t:sections.main-collection-banner.settings.show_collection_description.label"
    },
    {
      "type": "checkbox",
      "id": "show_collection_image",
      "default": false,
      "label": "t:sections.main-collection-banner.settings.show_collection_image.label",
      "info": "t:sections.main-collection-banner.settings.show_collection_image.info"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    }
  ]
}
{% endschema %}
